services:
  - type: web
    name: travelinsight
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: travelinsight_db
          property: connectionString
